

export const insideview = {
    opacity: '1',
    transform: 'scale(1)',
    transition:'.2s all ease-in'
  }

 export  const outside = {
    opacity: '0',
    transform: 'scale(.9)',
    // transition:'.3s all ease-in'
  }

